<!-- Generated from .project/PROJECT.md by the engine — do not run `impeccable init`, which would interview a user who isn't here. Edit .project/PROJECT.md; this file is regenerated from it. -->
# workspace

**Platform:** Responsive web — Next.js, React, Tailwind. shadcn/ui and lucide are installed and available; the direction decides whether they fit or whether this surface needs its own vocabulary.
**Task mode:** Operate (pass as `--mode operate` to any script)

## What this is

Build a complete, fully functional personal PWA web application called ARCHIVE — a private single-user archive for recording Books, Movies, TV Series, and Gallery/Museum/Exhibition visits. This is a full rebuild from scratch; every feature must work end-to-end (search, selection, manual entry, save, edit, delete, persistent storage, year filtering, TV season tracking, API error handling, PWA behavior). No placeholder functionality. PHILOSOPHY: Intentionally simple. NOT a review/rating/social/stats app. Do NOT add: ratings, stars, scores, reviews, notes, comments, statistics, charts, recaps, recommendations, AI, social, tags, favorites, collections, reading/movie progress, episode-level tracking, genre filters, dashboards, maps, gamification. Do not invent extra features. DESIGN: Mobile-first for Apple iPhone 16 portrait (~393 CSS px). iOS/iMessage-inspired but not an exact clone: clean, minimal, white background, light iOS gray, iOS blue accents (#007AFF), near-black text, subtle separators, rounded controls, minimal shadows, no gradients, no glassmorphism, no giant cards, no dashboard aesthetic. System font stack (-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display", "Helvetica Neue", Arial, sans-serif). No horizontal overflow, respect iOS safe areas (env(safe-area-inset-top/bottom)), comfortable touch targets, floating button must not overlap Safari controls/home indicator, desktop stays compact. PWA: Web app manifest, standalone display, app icons, mobile metadata, viewport setup; feels natural from iPhone Home Screen. HEADER: Top-left exact text "ARCHIVE" (uppercase, bold, no subtitle — never "PERSONAL ARCHIVE"). Top-right: year dropdown (e.g. 2026⌄). Compact header. YEAR SYSTEM: Default = current calendar year (do NOT hardcode). User can switch years and add past/future years. Every record stores archiveYear. Switching year immediately updates all four counts and the list; active category stays selected. CATEGORY TABS below header: Book, Movie, Series, Gallery — horizontal, fit iPhone 16, count under each label for selected year. Restrained iOS-style active emphasis. No separate pages unless technically necessary. MAIN LIST: vertically scrollable iOS-native-style rows for selected year + active category, subtle separators, no Pinterest cards/grids, no oversized thumbnails. FLOATING ADD: circular blue + bottom-right, fixed, respects bottom safe area, large touch target. Behavior follows active tab: Book→Add Book, Movie→Add Movie, Series→Add Series, Gallery→Add Gallery. No category picker. ADD-FLOW UX (Book/Movie/Series): default is DATABASE SEARCH MODE. Initial modal contains only: search input, search results area, "Enter Manually" button, Cancel. Never show manual fields in the initial search view. Flows: (A) search → select result → editable imported metadata form → Save; (B) "Enter Manually" → manual form → Save; (C) Cancel → close without saving. BOOK SEARCH: Kakao/Daum Book Search REST API — GET https://dapi.kakao.com/v3/search/book with header Authorization: KakaoAK ${KAKAO_REST_API_KEY}. Env var KAKAO_REST_API_KEY, server-side route only (e.g. /api/books/search) — never expose the key to the browser. NO Naver API. Params: query, sort=accuracy, page=1, size=20. Korean-titled searches. Results show cover thumbnail, Korean title, author(s), publisher; editions distinguishable. Mapping: title→title, authors→author, publisher→publisher, thumbnail→cover, isbn→ISBN, datetime→publication date, translators→translators (internal only). Row shows [cover] / Title / Author / Publisher, Korean metadata, title strongest. Manual entry fields: title, author, publisher, cover URL (title required; handle missing author/publisher gracefully). After selecting a Kakao result: populate editable form (title, author, publisher, cover editable), Save then local record is source of truth — never refetch/overwrite. Edge cases: multiple authors (render naturally, not JSON), missing cover/publisher/author, empty results, invalid ISBN, ISBN10+13 combined, network errors, Kakao API errors. MOVIE SEARCH: TMDB API. Preferred credential TMDB_ACCESS_TOKEN (Bearer token); optional TMDB_API_KEY. Secure backend route /api/movies/search. Search mode; results show poster, English title, release year. On selection fetch details+credits, extract director name(s) (English), then editable confirmation form (title, year, director, poster editable) → Save. Row: [poster] / English title / release year / director(s). Manual entry: English title, release year, director name(s), poster URL (title required). SERIES SEARCH: TMDB TV API via /api/series/search. Results show poster, English title, year. On selection fetch TV details, retrieve seasons, EXCLUDE Season 0 / Specials / non-standard special seasons, create numbered seasons, then editable confirmation form (title, poster, season structure editable) → Save. Row: [poster] / English title / season progress boxes (□ unwatched, ■ watched). CRITICAL season model: each season has independent watched state, e.g. [{seasonNumber:1,watched:true},{seasonNumber:2,watched:true},{seasonNumber:3,watched:false},...] rendering as ■ ■ □ — never store only "3/5". Season boxes are clickable directly from the main list: tap toggles watched, updates UI immediately, persists immediately, no edit screen needed. Accessible controls (aria labels/roles), do not rely on color alone. Manual entry: English title, poster URL, number of normal seasons N → auto-create seasons 1..N all watched=false. Preserve watched states during later edits where possible. GALLERY: manual entry only, NO external API. + opens manual form directly. Fields: City (required), Museum/Gallery (required), Exhibition (optional). Row: City / Museum (prominent) / Exhibition — omit exhibition line entirely when empty (never "No exhibition", "-", "None"). CANCEL/SAVE/EDIT/DELETE: every flow has working Cancel that discards temp data, closes, changes nothing. Save: validate required fields, unique id, attach archiveYear + category, persist, close modal, update list AND count immediately, no refresh. All records editable via tapping the row → edit sheet (Book: title/author/publisher/cover; Movie: title/year/director/poster; Series: title/poster/seasons; Gallery: city/museum/exhibition); edits persist immediately; no permanent row Edit buttons. All records deletable (swipe-to-delete or delete inside edit screen, with confirmation); deletion removes from storage and list and updates count immediately. DATA MODEL: common fields id, archiveYear, category, createdAt, updatedAt. Book: title, author, publisher, coverUrl, isbn, externalSource, externalId. Movie: titleEnglish, releaseYear, directorNames, posterUrl, tmdbId. Series: titleEnglish, posterUrl, tmdbId, seasons[{seasonNumber,watched}]. Gallery: city, museum, exhibition(nullable). PERSISTENCE: data survives refresh, browser restart, Safari close/reopen, PWA reopen — real persistent storage/database (e.g. Supabase or local persistent DB), not in-memory state. Single-user, no authentication/multi-user. ENVIRONMENT: server-side env vars TMDB_ACCESS_TOKEN, TMDB_API_KEY (optional), KAKAO_REST_API_KEY. Provide .env.example with exactly: TMDB_ACCESS_TOKEN= / TMDB_API_KEY= / KAKAO_REST_API_KEY= . Never hardcode keys, never expose secrets in frontend bundle, never log secrets. Use these actual credentials in the environment config only (not in client code): TMDB_ACCESS_TOKEN: eyJhbGciOiJIUzI1NiJ9.eyJhdWQiOiI1ZTNhZjc1Mzk0NTlmODYwOGQ4Y2M4M2NjNTYyOTFlYiIsIm5iZiI6MTc4OTIxMTg2OS44NzEsInN1YiI6IjZhYTUzNGRkMjFjMDI0NjljMzM1ZGJkOCIsInNjb3BlcyI6WyJhcGlfcmVhZCJdLCJ2ZXJzaW9uIjoxfQ.89WU_gPJsBWTXa-KhKEAbFxS9WNqxWZxqMRCev3nF2I TMDB_API_KEY: 5e3af7539459f8608d8cc83cc56291eb KAKAO_REST_API_KEY: 75f28b51f8f327179337b7e531a45fc1 API ARCHITECTURE: Frontend → internal API routes → external APIs: /api/books/search→Kakao, /api/movies/search→TMDB, /api/series/search→TMDB. Secrets server-side only. LOADING/ERRORS: subtle loading states during search, prevent duplicate requests. Empty results → "No results found." with Enter Manually still available. Handle: missing/invalid credentials, network failure, external API errors, malformed responses, failed save, failed delete, missing images, missing metadata, empty results — concise readable messages, never silent, never crash. IMAGES: Kakao thumbnails / TMDB posters, consistent dimensions, object-fit: cover, subtle rounded corners, lazy loading, placeholder when missing, not oversized. MODALS: mobile-friendly iOS-style sheets; Book/Movie/Series initial Add = search field + results + Enter Manually + Cancel; after selection = editable form + Save + Cancel; manual mode = fields + Save + Cancel; Gallery = fields + Save + Cancel. No stacked/overlapping modals. ACCESSIBILITY: semantic HTML, keyboard-accessible buttons, ARIA labels, sufficient contrast, accessible season toggles, mobile-suitable touch targets. PERFORMANCE: debounced search input, optimized image loading and list rendering, no heavy libraries. EMPTY STATES: minimal text per category ("No books yet." etc.), keep + available. MAIN SCREEN: header ARCHIVE + year dropdown; tabs with counts; list rows [thumbnail] Title / secondary metadata; floating + bottom-right; Gallery text-only rows. FINAL: do not stop at planning or UI — install dependencies, configure routes, implement handlers and storage, wire all buttons/forms/API requests, run the app, test all interactions, and fix runtime/build errors until the complete app works end-to-end. Verify: app launches, header correct, year dropdown + add years work, four tabs work with per-year counts, + follows tab, no horizontal overflow at 393px, Kakao book search works with real results, TMDB movie search + director fetch works, TMDB series search + season creation (excluding specials) works, season toggles persist after refresh, manual entry works for all four categories, edit and delete work for all records, refresh/PWA-reopen preserves data, API failures don't crash, no secrets in frontend bundle, production build succeeds.

## Brand commitments & durable constraints

- **The user is present and IS choosing the visual direction.** Offer six materially different grounded directions through the design-picker command and STOP; the answer arrives as your next input and the chosen direction is then pinned, exactly as if the user had named it. Do not build a direction before asking, do not narrow the six to a favourite, and do not ask about anything else in the same breath — this one decision is the whole card.
- **Ask ONLY when the direction is genuinely open**: this project has no committed visual world yet, or the user asked to redesign the whole app. A later change inside a committed world inherits it — asking again there is an interruption, not a courtesy.
- **The roll decides which six you show, never which one wins.** Order your grounded directions by resonance, then use the assignment to pick the spread you present, so the lineup is not your own top six. Showing your favourites is the failure this whole mechanism exists to prevent.
- **All six options ARE dealt challengers — every one, and nothing else.** Do not put your own grounded directions on the card, do not invent a seventh world, and do not reword a challenger into something safer. Your derivation still matters: it is how you FUSE the world the user picks with this product. It just does not get to decide the menu.
- **Deal THREE rolls, then show the six that can carry this product.** Run the concept-seed command three times and pool every challenger it deals (about eighteen worlds). Drop the ones that cannot hold this product's actual task — a world whose grammar has nowhere to put the main thing the user does here is out, however beautiful its board. Keep the six strongest of what survives.
- **Fitness is the filter, never taste.** "Can this world's own grammar carry capture, or a list, or a form, or whatever this product mainly does?" is the question. "Would I have picked this?" is not, and a lineup of the six calmest worlds is the ranking rut wearing fitness as a disguise. A striking world that genuinely fuses beats a tame one every time.
- **The six must span at least THREE different material families.** Paper, ink and stationery are ONE family. If your surviving six are all screens, or all paper, you filtered by familiarity rather than by fit — go back to the pool.
- **Every option carries that challenger's QUALITY BAR *board* URL as `preview_image`, verbatim.** An option without one is dropped by the command, because the board is the only proof the world came from the roll. Six worlds, six boards. If the roll dealt fewer than six, show what it dealt.
- **Never generate images for the card.** The catalog already ships the artwork, and rendering sketches would make the user sit and wait to answer a question about which world — not which composition.
- **Also give every option a `swatch`** of the real colours you would build that world in, plus ONE plain sentence saying what this world does for THIS product — "a control-room rhythm for capturing thoughts fast" tells them something; listing the ground colour twice does not. The card already labels the board as a reference, so never spend the sentence saying that: "reference world, not a mockup" inside the description is wasted words on every card.
- The user's own words always outrank the roll. A direction, palette, face or reference they named is pinned; the roll only decides what they left open.
- Two review rounds is the budget, then ship and report open items honestly under the reviewer's own verdict — never announce a table with open findings as a pass.
- Web fonts load through Fontsource, never `next/font/google` — this sandbox has no Google egress, so the fetch hangs at compile and the preview renders blank.
- No AI-builder badge, watermark, or attribution anywhere in the product.
- Decisions recorded in `.project/` (ledger, DESIGN_SYSTEM.md) are commitments; contradict one only when the user asks.

## Positioning

Build a complete, fully functional personal PWA web application called ARCHIVE — a private single-user archive for recording Books, Movies, TV Series, and Gallery/Museum/Exhibition visits. This is a full rebuild from scratch; every feature must work end-to-end (search, selection, manual entry, save, edit, delete, persistent storage, year filtering, TV season tracking, API error handling, PWA behavior). No placeholder functionality. PHILOSOPHY: Intentionally simple. NOT a review/rating/social/stats app. Do NOT add: ratings, stars, scores, reviews, notes, comments, statistics, charts, recaps, recommendations, AI, social, tags, favorites, collections, reading/movie progress, episode-level tracking, genre filters, dashboards, maps, gamification. Do not invent extra features. DESIGN: Mobile-first for Apple iPhone 16 portrait (~393 CSS px). iOS/iMessage-inspired but not an exact clone: clean, minimal, white background, light iOS gray, iOS blue accents (#007AFF), near-black text, subtle separators, rounded controls, minimal shadows, no gradients, no glassmorphism, no giant cards, no dashboard aesthetic. System font stack (-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display", "Helvetica Neue", Arial, sans-serif). No horizontal overflow, respect iOS safe areas (env(safe-area-inset-top/bottom)), comfortable touch targets, floating button must not overlap Safari controls/home indicator, desktop stays compact. PWA: Web app manifest, standalone display, app icons, mobile metadata, viewport setup; feels natural from iPhone Home Screen. HEADER: Top-left exact text "ARCHIVE" (uppercase, bold, no subtitle — never "PERSONAL ARCHIVE"). Top-right: year dropdown (e.g. 2026⌄). Compact header. YEAR SYSTEM: Default = current calendar year (do NOT hardcode). User can switch years and add past/future years. Every record stores archiveYear. Switching year immediately updates all four counts and the list; active category stays selected. CATEGORY TABS below header: Book, Movie, Series, Gallery — horizontal, fit iPhone 16, count under each label for selected year. Restrained iOS-style active emphasis. No separate pages unless technically necessary. MAIN LIST: vertically scrollable iOS-native-style rows for selected year + active category, subtle separators, no Pinterest cards/grids, no oversized thumbnails. FLOATING ADD: circular blue + bottom-right, fixed, respects bottom safe area, large touch target. Behavior follows active tab: Book→Add Book, Movie→Add Movie, Series→Add Series, Gallery→Add Gallery. No category picker. ADD-FLOW UX (Book/Movie/Series): default is DATABASE SEARCH MODE. Initial modal contains only: search input, search results area, "Enter Manually" button, Cancel. Never show manual fields in the initial search view. Flows: (A) search → select result → editable imported metadata form → Save; (B) "Enter Manually" → manual form → Save; (C) Cancel → close without saving. BOOK SEARCH: Kakao/Daum Book Search REST API — GET https://dapi.kakao.com/v3/search/book with header Authorization: KakaoAK ${KAKAO_REST_API_KEY}. Env var KAKAO_REST_API_KEY, server-side route only (e.g. /api/books/search) — never expose the key to the browser. NO Naver API. Params: query, sort=accuracy, page=1, size=20. Korean-titled searches. Results show cover thumbnail, Korean title, author(s), publisher; editions distinguishable. Mapping: title→title, authors→author, publisher→publisher, thumbnail→cover, isbn→ISBN, datetime→publication date, translators→translators (internal only). Row shows [cover] / Title / Author / Publisher, Korean metadata, title strongest. Manual entry fields: title, author, publisher, cover URL (title required; handle missing author/publisher gracefully). After selecting a Kakao result: populate editable form (title, author, publisher, cover editable), Save then local record is source of truth — never refetch/overwrite. Edge cases: multiple authors (render naturally, not JSON), missing cover/publisher/author, empty results, invalid ISBN, ISBN10+13 combined, network errors, Kakao API errors. MOVIE SEARCH: TMDB API. Preferred credential TMDB_ACCESS_TOKEN (Bearer token); optional TMDB_API_KEY. Secure backend route /api/movies/search. Search mode; results show poster, English title, release year. On selection fetch details+credits, extract director name(s) (English), then editable confirmation form (title, year, director, poster editable) → Save. Row: [poster] / English title / release year / director(s). Manual entry: English title, release year, director name(s), poster URL (title required). SERIES SEARCH: TMDB TV API via /api/series/search. Results show poster, English title, year. On selection fetch TV details, retrieve seasons, EXCLUDE Season 0 / Specials / non-standard special seasons, create numbered seasons, then editable confirmation form (title, poster, season structure editable) → Save. Row: [poster] / English title / season progress boxes (□ unwatched, ■ watched). CRITICAL season model: each season has independent watched state, e.g. [{seasonNumber:1,watched:true},{seasonNumber:2,watched:true},{seasonNumber:3,watched:false},...] rendering as ■ ■ □ — never store only "3/5". Season boxes are clickable directly from the main list: tap toggles watched, updates UI immediately, persists immediately, no edit screen needed. Accessible controls (aria labels/roles), do not rely on color alone. Manual entry: English title, poster URL, number of normal seasons N → auto-create seasons 1..N all watched=false. Preserve watched states during later edits where possible. GALLERY: manual entry only, NO external API. + opens manual form directly. Fields: City (required), Museum/Gallery (required), Exhibition (optional). Row: City / Museum (prominent) / Exhibition — omit exhibition line entirely when empty (never "No exhibition", "-", "None"). CANCEL/SAVE/EDIT/DELETE: every flow has working Cancel that discards temp data, closes, changes nothing. Save: validate required fields, unique id, attach archiveYear + category, persist, close modal, update list AND count immediately, no refresh. All records editable via tapping the row → edit sheet (Book: title/author/publisher/cover; Movie: title/year/director/poster; Series: title/poster/seasons; Gallery: city/museum/exhibition); edits persist immediately; no permanent row Edit buttons. All records deletable (swipe-to-delete or delete inside edit screen, with confirmation); deletion removes from storage and list and updates count immediately. DATA MODEL: common fields id, archiveYear, category, createdAt, updatedAt. Book: title, author, publisher, coverUrl, isbn, externalSource, externalId. Movie: titleEnglish, releaseYear, directorNames, posterUrl, tmdbId. Series: titleEnglish, posterUrl, tmdbId, seasons[{seasonNumber,watched}]. Gallery: city, museum, exhibition(nullable). PERSISTENCE: data survives refresh, browser restart, Safari close/reopen, PWA reopen — real persistent storage/database (e.g. Supabase or local persistent DB), not in-memory state. Single-user, no authentication/multi-user. ENVIRONMENT: server-side env vars TMDB_ACCESS_TOKEN, TMDB_API_KEY (optional), KAKAO_REST_API_KEY. Provide .env.example with exactly: TMDB_ACCESS_TOKEN= / TMDB_API_KEY= / KAKAO_REST_API_KEY= . Never hardcode keys, never expose secrets in frontend bundle, never log secrets. Use these actual credentials in the environment config only (not in client code): TMDB_ACCESS_TOKEN: eyJhbGciOiJIUzI1NiJ9.eyJhdWQiOiI1ZTNhZjc1Mzk0NTlmODYwOGQ4Y2M4M2NjNTYyOTFlYiIsIm5iZiI6MTc4OTIxMTg2OS44NzEsInN1YiI6IjZhYTUzNGRkMjFjMDI0NjljMzM1ZGJkOCIsInNjb3BlcyI6WyJhcGlfcmVhZCJdLCJ2ZXJzaW9uIjoxfQ.89WU_gPJsBWTXa-KhKEAbFxS9WNqxWZxqMRCev3nF2I TMDB_API_KEY: 5e3af7539459f8608d8cc83cc56291eb KAKAO_REST_API_KEY: 75f28b51f8f327179337b7e531a45fc1 API ARCHITECTURE: Frontend → internal API routes → external APIs: /api/books/search→Kakao, /api/movies/search→TMDB, /api/series/search→TMDB. Secrets server-side only. LOADING/ERRORS: subtle loading states during search, prevent duplicate requests. Empty results → "No results found." with Enter Manually still available. Handle: missing/invalid credentials, network failure, external API errors, malformed responses, failed save, failed delete, missing images, missing metadata, empty results — concise readable messages, never silent, never crash. IMAGES: Kakao thumbnails / TMDB posters, consistent dimensions, object-fit: cover, subtle rounded corners, lazy loading, placeholder when missing, not oversized. MODALS: mobile-friendly iOS-style sheets; Book/Movie/Series initial Add = search field + results + Enter Manually + Cancel; after selection = editable form + Save + Cancel; manual mode = fields + Save + Cancel; Gallery = fields + Save + Cancel. No stacked/overlapping modals. ACCESSIBILITY: semantic HTML, keyboard-accessible buttons, ARIA labels, sufficient contrast, accessible season toggles, mobile-suitable touch targets. PERFORMANCE: debounced search input, optimized image loading and list rendering, no heavy libraries. EMPTY STATES: minimal text per category ("No books yet." etc.), keep + available. MAIN SCREEN: header ARCHIVE + year dropdown; tabs with counts; list rows [thumbnail] Title / secondary metadata; floating + bottom-right; Gallery text-only rows. FINAL: do not stop at planning or UI — install dependencies, configure routes, implement handlers and storage, wire all buttons/forms/API requests, run the app, test all interactions, and fix runtime/build errors until the complete app works end-to-end. Verify: app launches, header correct, year dropdown + add years work, four tabs work with per-year counts, + follows tab, no horizontal overflow at 393px, Kakao book search works with real results, TMDB movie search + director fetch works, TMDB series search + season creation (excluding specials) works, season toggles persist after refresh, manual entry works for all four categories, edit and delete work for all records, refresh/PWA-reopen preserves data, API failures don't crash, no secrets in frontend bundle, production build succeeds.

## Operating Context

Responsive web, built unattended in one pass. Task mode: Operate.

## Product Principles

- Clarity of the task beats decoration; the interface earns attention only where the product does.
- Say what is true: no invented prices, customers, benchmarks or capabilities the product does not have.
